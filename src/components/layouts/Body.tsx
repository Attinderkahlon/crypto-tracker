import StandardCard from "../cards/StandardCard";

const Body = () => {
  return (
    <div>
      <h1 className="my-6">Explore blockchains</h1>
      <StandardCard />
    </div>
  );
};

export default Body;
